import {render, screen} from "@testing-library/react";
import ModalForm from "../../../components/sidebar/modal-form";
import userEvent from '@testing-library/user-event'
import React from "react";
test("change caption", async () => {
    // const setState = jest.fn();
    // jest
    // .spyOn(React, 'useState')
    // .mockImplementationOnce(initState => [initState, setState]);

    // render(<ModalForm caption={caption} setCaption={setCaption}/>)

    // const inputTextBefore = screen.getByTestId("input-text");
    // await userEvent.type(inputTextBefore,"Hello World!");
    // const inputTexAfter = screen.getByTestId("input-text");
    // expect(inputTexAfter).toHaveTextContent("Hello World!");
})